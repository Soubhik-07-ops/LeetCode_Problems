# Image Overlap

- **Difficulty:** Medium
- **Language:** python3
- **Topics:** Array, Matrix
- **Submission ID:** 2140723069
- **Status:** accepted
- **Date:** 2026-09-13T15:05:28.732000+00:00

## Source Code

```
class Solution:
    def largestOverlap(self, img1: List[List[int]], img2: List[List[int]]) -> int:
        n = len(img1)
        ones1 = [(i, j) for i in range(n) for j in range(n) if img1[i][j]]
        ones2 = [(i, j) for i in range(n) for j in range(n) if img2[i][j]]
    
        count = Counter()
        for r1, c1 in ones1:
            for r2, c2 in ones2:
                count[(r2-r1, c2-c1)] += 1
            
        return max(count.values()) if count else 0
```
